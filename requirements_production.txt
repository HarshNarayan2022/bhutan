# Production Requirements for Render - 512MB RAM Optimized
# Flask + FastAPI architecture - ESSENTIAL PACKAGES ONLY

# Core web frameworks
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Werkzeug==2.3.7
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Production servers
gunicorn==21.2.0

# Proxy server
aiohttp==3.9.1

# Database
SQLAlchemy==2.0.23
psycopg2-binary==2.9.7

# Environment management
python-dotenv==1.0.0

# HTTP requests
requests==2.31.0
httpx==0.25.1

# Security
bcrypt==4.1.2

# Essential AI/ML (lightweight, open source only)
# Removed: openai (closed source)

# Open source alternatives for lightweight ML
scikit-learn==1.3.0
spacy==3.6.1

# Open source speech recognition (lightweight)
vosk==0.3.45
soundfile==0.12.1

# For audio processing in Vosk
scipy==1.11.1

# Basic ML/Data processing (minimal versions)
numpy==1.24.4

# Visualization (required by FastAPI)
matplotlib==3.7.2

# Basic NLP without heavy models
nltk==3.8.1
# Sentence transformers (lightweight)
# TTS (lightweight)
edge-tts==6.1.9

# PDF generation (lightweight)
reportlab==4.0.4

# Async support
aiofiles==23.2.1

# Utilities
PyYAML==6.0.1
Pillow==10.1.0

# JSON handling
orjson>=3.9.0

# CORS middleware
flask-cors==4.0.0

# Pydantic for data validation
pydantic==2.5.1
